/ Content Header (Page header)
%section.content-header
  %h1
    Deployments
  %ol.breadcrumb
    %li= link_to "Deployments", dynamic_url(@current_application, :deployments)
    %li.active New Deployment
%section.content
  .row
    .col-lg-12.connectedSortable
      .box
        .box-header.with-border
          %h3.box-title Add a new deployment event.
        .box-body
          = render "form"
